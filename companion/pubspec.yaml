name: paceturner_companion
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Gate V2 spike transport (Story 1.4) — pins Garmin Android SDK 2.0.3
  # internally; OQ2 (docs/decisions/0002) decides whether it stays for Epic 4.
  watch_connectivity_garmin: ^0.1.13

  # Unicode NFC normalization for the import pipeline (Story 2.1). Pure-Dart
  # port of unorm.js — zero deps, no Flutter/platform code → runs in the import
  # isolate (AR19). Dart core String has no built-in normalize().
  unorm_dart: 0.3.2

  # State management (Story 2.3) — arch-pinned (architecture.md:202).
  flutter_riverpod: ^3.3.1

  # Local metadata store (Story 2.3): drift owns Books/Chapters rows; bulk
  # word-stream bytes live as flat files (stream_store). Stable native setup
  # (drift + sqlite3_flutter_libs + path_provider + LazyDatabase) — chosen over
  # the drift_flutter ^0.3.1-wip pre-release (won't resolve; Task 0 decision).
  drift: ^2.34.0
  sqlite3_flutter_libs: ^0.5.40
  path_provider: ^2.1.5

  # Import entry points (Story 2.3): file picker for the testable spine.
  file_picker: ^8.1.4

  # Markdown import (Story 2.3): md → HTML (markdown) → plain text (html). Both
  # are pure Dart so html_extractor runs inside the compute isolate (AR19).
  markdown: ^7.3.0
  html: ^0.15.5

  # Share-sheet import entry (Story 2.3, Task 8 / AC6): receives .txt/.md shared
  # from other apps and routes them through the same importFile path.
  receive_sharing_intent: ^1.8.0

  # EPUB parsing (Story 2.4): OCF/OPF/spine/TOC → real chapters + decoded cover.
  # Pure Dart (no dart:io) — transitively pulls archive/xml/image/collection,
  # all pure Dart — so epub_extractor runs inside the compute isolate (AR19).
  epub_pro: ^5.6.0

  # Cover re-encode/downscale (Story 2.4, cover_extractor): decoded img.Image →
  # bounded JPEG bytes. Pure Dart (epub_pro's own cover dep) — declared directly
  # since cover_extractor imports it (depend_on_referenced_packages).
  image: ^4.8.0

  # @visibleForTesting for epub_extractor's metadata sanitizer (Story 2.4 review
  # patch). Declared directly since epub_extractor imports it; already present
  # transitively (depend_on_referenced_packages).
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # On-device import verification (Story 2.4 manual hardware check). Drives the
  # real ImportService against a real EPUB staged in the app's external files
  # dir — exercises the real compute isolate + sqlite + cover-file write.
  integration_test:
    sdk: flutter

  # drift code generation (Story 2.3) — generates database.g.dart.
  drift_dev: ^2.34.0
  build_runner: ^2.15.0

  # Synthetic EPUB test-fixture builder (Story 2.4, Task 1) assembles OCF zips
  # in memory via ZipEncoder. Test-only (epub_pro's own transitive dep).
  archive: ^4.0.9

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
